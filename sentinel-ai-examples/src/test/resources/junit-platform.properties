# Disable parallel execution for the examples module.
# Tests in this module start embedded servers (Dropwizard, Jetty) in background threads.
# Running multiple embedded servers concurrently in the same JVM causes "VM terminated without
# properly saying goodbye" Surefire crashes due to leftover daemon threads on JVM shutdown.
junit.jupiter.execution.parallel.enabled=false
